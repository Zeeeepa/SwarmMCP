apiVersion: v1
kind: Service
metadata:
  name: unified-mcp-server
  labels:
    app: unified-mcp-server
spec:
  selector:
    app: unified-mcp-server
  ports:
  - port: 80
    targetPort: 3000
    name: http
  type: ClusterIP

